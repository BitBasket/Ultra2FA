{
  "dependencies": {
    "jquery": "^4.0.0-beta.2",
    "otplib": "^12.0.1"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build --no-optimize src/index.html"
  },
  "targets":{
    "main": {
       "includeNodeModules": true
    }
 }
}
